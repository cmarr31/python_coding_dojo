def compare(lst_one, lst_two):
	if (lst_one == lst_two):
		print "The lists are the same."
	else:
		print "The lists are NOT the same."

compare([1,2,5,6,2], [1,2,5,6,2])
compare([1,6,2], [1,2,5,6,2])